# Вступительная видеозаставка — 09.09.2026

После выбора любого героя `mission_01_intro.ogv` проигрывается целиком со звуком, затем автоматически начинается первая волна. Вступительные сюжетные карточки и кнопка пропуска удалены. Во время заставки бой, таймер миссии и игровая музыка стоят на паузе. Кадр показывается полностью с сохранением пропорций.

В кооперативе оба телефона показывают тот же ролик и автоматически подтверждают окончание. Первый досмотревший ждёт второго; при потере связи воспроизведение приостанавливается и продолжается с того же места. Повтор миссии повторно запускает видео. Выход из комнаты отменяет заставку без запоздалого запуска боя.

Источник: `hf_20260908_215728_0e6059f0-f4be-44eb-a2b0-5ce1990201c2.mp4`, копия сохранена в `source_art/intro/mission_01_20260909.mp4`. Исходные параметры: 1080×1920, H.264/AAC, 24 кадра/с, 15,093 секунды, 74 605 736 байт. Игровая версия: 720×1280, Ogg Theora/Vorbis, 24 кадра/с, та же длительность со всем исходным звуком, 18 724 911 байт. Видеоряд не обрезан и не ускорен.

Команда локального преобразования:

```powershell
.tools/ffmpeg.exe -i source_art/intro/mission_01_20260909.mp4 -map 0:v:0 -map 0:a:0 -vf "scale=720:1280:flags=lanczos,setsar=1" -c:v libtheora -q:v 6 -pix_fmt yuv420p -c:a libvorbis -q:a 5 assets/videos/story/mission_01_intro.ogv
```

Неиспользуемые теперь картинки `assets/story/bank_*` исключены из игровых пакетов, исходные PNG и архив для пользователя сохранены. Это оставляет пакет с новым видео в пределах 100 МиБ.

Проверки: `tests/intro_video_test.gd` — полный естественный просмотр за Гришу и Мутки, звук, отсутствие пропуска и прежних слайдов, переход прямо в бой, свежий таймер и первая волна; два экземпляра игры с сетевой паузой, ожиданием второго игрока, повтором и выходом во время заставки. `tests/mission_flow_test.gd -- --mutki` и `tests/coop_mission_test.gd` — полные миссии, результаты, повтор и поражение. Реальные кадры видео и боя сохранены в `artifacts/intro_video_20260909/`.

Все три проверки завершились `PASS`. В существующих тестах миссий при выходе остаются сообщения Godot о ресурсах; среда также сообщает о системном хранилище сертификатов. Проверка самого вступления после исправления возврата музыки завершилась без игровых ошибок.

Windows-пакет и обе браузерные копии совпадают по SHA-256: `ae009d4220a46997a378d028e75278c210fac64dc6a93e1742cb5ebc4b847b6f`, размер — 101 272 436 байт (96,58 МиБ). В `docs/` подключён `index-ae009d4220a4.pck`. Исходный MP4 сверён с копией по SHA-256. Отчёт: `artifacts/intro_video_20260909/build-verification.json`.

В готовой браузерной сборке проверены настоящие нажатия: одиночный режим → Гриша → начало видео, Escape и нажатие на прежнее место пропуска → видео продолжается → без дополнительных нажатий начинается бой. Ошибок и предупреждений в консоли браузера нет. Windows-пакет проверен запуском через `--main-pack`.

Интернет-проверка `tests/coop_browser_test.cjs` остановилась до создания комнаты: сервис PeerJS не ответил. Полный кооператив через интернет в этой проверке не подтверждён; локальная проверка двух экземпляров прошла. Журнал: `artifacts/intro_video_20260909/browser-test.log`. Сборки обновлены локально, публикация на GitHub Pages не выполнялась.
